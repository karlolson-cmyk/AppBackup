# 2026-06-30 Work Log

## Appkitz v2.8.0 Release

### 第一次构建（闪退问题）
1. **版本升级**: app/build.gradle.kts 中 versionCode → 22, versionName → "2.8.0"
2. **构建 Release APK**: 启用 R8 混淆 (`isMinifyEnabled = true`, `isShrinkResources = true`)，APK 7.1 MB
3. **问题**: release APK 闪退，因 ProGuard 规则不完善导致运行时反射代码被错误移除

### 修复构建
4. **关闭混淆**: `isMinifyEnabled = false`, `isShrinkResources = false`，避免 R8 裁剪导致闪退
5. **重新 clean + assembleRelease**: APK 26.6 MB（未混淆，体积增大但功能完整）
6. **删除旧 Release**: `gh release delete v2.8.0 --yes --cleanup-tag`
7. **重新推送代码并创建 Release**: https://github.com/karlolson-cmyk/Appkitz/releases/tag/v2.8.0

### 经验
- release 闪退多为 R8/ProGuard 混淆导致，需完善 keep 规则或直接关闭混淆
- debug 构建不启用混淆，所以不会复现此类问题

## Appkitz v2.9.0 Release（优化体积）

### 问题
- v2.8.0 关闭混淆后 APK 体积 26.6MB，用户要求压缩到 15MB 以内

### 解决方案
1. **重新启用 R8 混淆** (`isMinifyEnabled = true`, `isShrinkResources = true`)
2. **精简 ProGuard 规则**: 不手动 keep 整个库包，让库自带的 consumer rules 处理
   - 只 keep `com.appkitz.**`（应用代码）和 `com.reandroid.**`（ARSCLib，无 consumer rules）
   - 对 OkHttp/OkIO/ErrorProne 只用 `-dontwarn`，不用 `-keep`
   - Compose/AndroidX 自带 consumer ProGuard rules，无需手动 keep
3. **补充缺失类规则**: `com.google.errorprone.annotations.**` 的 `-dontwarn`（Tink/SecurityCrypto 引用）
4. **结果**: APK 6.9MB（从 26.6MB 降至 6.9MB，降幅 74%）

### 关键经验
- 现代 Android 库（Compose, OkHttp, AndroidX 等）自带 consumer ProGuard rules，不需要手动 keep
- 手动 `-keep class xxx.** { *; }` 会阻止 R8 裁剪，导致体积膨胀
- 只需 keep 无 consumer rules 的库（如 ARSCLib）和应用自身代码
- R8 missing_rules.txt 会提示需要添加的 `-dontwarn` 规则

## Appkitz v2.9.0 最终版（-dontobfuscate 方案）

### 问题
- 之前的 v2.9.0 启用混淆后闪退（R8 重命名导致反射失败）
- 不混淆版本 26.6MB 太大

### 最终方案
1. **启用 R8 tree-shaking**: `isMinifyEnabled = true`, `isShrinkResources = true`
2. **禁用混淆**: `-dontobfuscate` — 保留所有类名/方法名，避免反射崩溃
3. **禁用优化**: `-dontoptimize` — 不做代码变换
4. **精简 keep 规则**: 只 keep app 代码 + ARSCLib，其他靠库自带 consumer rules
5. **使用 proguard-android.txt**（非 optimize 版本）

### 结果
- APK: **9.9MB**（目标 10MB 以内）
- 不闪退（无混淆 = 无反射问题）
- 从 26.6MB 压缩至 9.9MB，降幅 63%

### 核心经验
- `-dontobfuscate` 是安全压缩的关键：R8 只做 tree-shaking（移除未使用代码），不重命名
- Compose 等库使用大量运行时反射，混淆后即使有 keep 规则也可能崩溃
- tree-shake + shrinkResources 已经能大幅减小体积，obfuscate 只是锦上添花但风险高
